[pytest]
markers =
    tier1: Quick benchmarks (~30s) — tiny/small files, fast operations. Run during development.
    tier2: Medium benchmarks (~2min) — medium files, most features. Run before commits.
    tier3: Full benchmarks (~10min+) — large files, batch, wrapped. Run before releases.
